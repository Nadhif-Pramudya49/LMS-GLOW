# CLAUDE.md

## Project Context
- **Name**: Library Management System (LMS)
- **Stack**: React (Frontend), Node.js + Express (Backend), MySQL (Database)
- **Workflow**: GSD Core (Discuss -> Plan -> Execute -> Verify -> Ship)

## Development Rules
- Ikuti standar PSR untuk dokumentasi (jika ada).
- Gunakan JWT untuk keamanan.
- Pastikan semua perubahan kode memiliki rencana di `.planning/`.

## Shared Conventions
- API base path: `/api/v1`
- Folder structure:
  - `/client`: React app
  - `/server`: Node.js app
  - `/docs`: Project documentation
